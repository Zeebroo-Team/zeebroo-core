# Dependencies
node_modules/

# Build output
dist/
out/

# Electron builder cache
.electron-builder-cache/

# Playwright driver
driver.mjs

# macOS
.DS_Store

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Runtime config written by the app
renderer/config.json

# Build assets — plist and icons are all committed
# (icon files are binary; keep them tracked so CI/CD builds work)

# User data / local state
*.key

# Lock files (optional — remove this line if you want to commit package-lock.json)
# package-lock.json
